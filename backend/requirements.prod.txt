# ============================
# Production runtime dependencies
# ============================
# Excludes: chromadb, kokoro, faster-whisper, llama-cpp-python, opencv,
#           mediapipe, spacy, webrtc stack, matplotlib.
# CPU-only torch is installed separately in the Dockerfile from
# https://download.pytorch.org/whl/cpu to keep the image small.

# Build essentials
setuptools>=68.0.0
wheel>=0.41.0

# Core backend
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0
pydantic==2.5.0
pydantic-core==2.14.1
python-dotenv==1.0.0
python-multipart==0.0.6
httpx==0.27.2
aiohttp==3.9.1
requests==2.31.0
anyio==3.7.1

# Numerical
numpy==1.26.4
pandas==2.2.2

# Embeddings (torch is installed CPU-only via Dockerfile)
sentence-transformers==2.2.2
transformers==4.34.0
tokenizers==0.14.1
huggingface-hub==0.17.3
safetensors==0.4.3

# RAG (admin-flagged ingestion + chunking)
langchain==0.1.0
langchain-core==0.1.23
langchain-community==0.0.10
pypdf==3.17.4
beautifulsoup4==4.12.2

# Vector DB — Qdrant Cloud primary, Chroma only to satisfy module-level imports
qdrant-client==1.11.3
chromadb==0.5.3

# Databases & auth
motor==3.6.0
pymongo==4.9.2
python-jose==3.3.0

# Google OAuth
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
cachetools==5.5.0
rsa==4.9

# LLM
groq==0.9.0

# PDF export
reportlab==4.0.9
pillow==10.4.0

# Utilities
tenacity==8.5.0
tqdm==4.66.5
regex==2024.9.11
PyYAML==6.0.3

# HTTP internals (pinned to avoid resolver drift)
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer==3.3.2
idna==3.10
h11==0.16.0
websockets==12.0
